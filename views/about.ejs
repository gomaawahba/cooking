<h3 class="pb-4">These chefs are dedicated to creating exceptional dishes that not only taste delicious but also showcase their creativity and attention to detail. They have honed their skills through years of experience and continuous learning, always striving to improve
    and innovate in the kitchen</h3>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item"><a href="/about">about</a></li>
        <li class="breadcrumb-item active" aria-current="page">ExploreAbout</li>
    </ol>
</nav>

<div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

    <% if(chefs != '') { %>
        <% chefs.forEach(function(chef, index){ %>
            <a href="/about/<%= chef._id %>" class="col text-center category__link">
                <div class="category__img category__img--large shadow">
                    <img src="/chef/<%= chef.image %>" alt="<%= chef.name %>" loading="lazy">
                </div>
                <div class="pt-1">
                    <%= chef.name %>
                </div>
            </a>
            <% }) %>
                <% } else { %>
                    <p>No items found.</p>
                    <% } %>
</div>